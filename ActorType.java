/**
 * An enum used to represent the type of actor so that
 * tiles can reject movement based on type.
 * @author sam
 * @see Tile
 */
public enum ActorType {
    BUTTERFLY,
    AMOEBA,
    BOULDER,
    DIAMOND,
    PLAYER,
    FROG,
    FIREFLY,
}
